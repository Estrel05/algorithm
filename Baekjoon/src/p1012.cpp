#include <iostream>
#include <vector>
using namespace std;

bool cabbage_map_maker() {
    int m, n, k, x, y;

    cin >> m >> n >> k;

    for (int i = 0; i < k; i++) {
        cin >> x >> y;
    }
}