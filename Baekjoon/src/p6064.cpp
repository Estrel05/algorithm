#include <iostream>
using namespace std;

/*
int lcm(int x, int y) {
    int a, b, r;

    a = x;
    b = y;
    while (y != 0) {
        r = x % y;
        x = y;
        y = r;
    }
    return a * b / x;
}
*/

int main() {
    int t, m, n, x, y;

    cin >> t;
    for (int i = 0; i < t; i++) {
        cin >> m >> n >> x >> y;
    }
}